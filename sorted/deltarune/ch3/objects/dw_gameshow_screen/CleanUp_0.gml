if (surface_exists(bg_surface))
    surface_free(bg_surface);
if (sprite_exists(bonus_sprite))
    sprite_delete(bonus_sprite);
if (sprite_exists(bonus_confirmed_sprite))
    sprite_delete(bonus_confirmed_sprite);
