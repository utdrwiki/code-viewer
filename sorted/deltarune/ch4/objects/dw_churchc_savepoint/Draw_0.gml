if (scr_debug())
{
    var count = 0;
    var space = 10;
    var border = 8;
    var _cx = camerax();
    var _cy = cameray();
}
with (shop)
{
    if (myinteract > 4 || (myinteract != 0 && instance_exists(obj_choicer_neo)))
        scr_shopspace_displaybox("item", 413, 161, true, true);
}
