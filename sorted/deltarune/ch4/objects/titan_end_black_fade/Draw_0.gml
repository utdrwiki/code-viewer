depth = -9999;
with (obj_jump_to_point)
{
    depth = -99999;
    ignoredepth = 1;
}
with (obj_herosusie)
    depth = -99999;
with (obj_titan_susiekris_jump)
    depth = -99999;
if (image_alpha < 0.7)
    image_alpha += 0.02;
