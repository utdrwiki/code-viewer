if (specialmode)
{
    if (sunkus_kb_check_pressed(84))
        screen_save("sprite_remove_" + string(n) + ".PNG");
    if (sunkus_kb_check_pressed(32))
        screen_save("sprite_translate_" + string(n) + ".PNG");
}
