scr_populatechars();
if (!init)
{
    with (obj_caterpillarchara)
    {
        if (name == "susie")
            target = 9;
        if (name == "ralsei")
            target = 18;
    }
    init = 1;
}
