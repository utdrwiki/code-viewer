xoff = ((__view_get(e__VW.XView, 0) + auto) * ss) + x;
yoff = ((__view_get(e__VW.YView, 0) + autoy) * ssy) + y;

enum e__VW
{
    XView,
    YView,
    WView,
    HView,
    Angle,
    HBorder,
    VBorder,
    HSpeed,
    VSpeed,
    Object,
    Visible,
    XPort,
    YPort,
    WPort,
    HPort,
    Camera,
    SurfaceID
}
