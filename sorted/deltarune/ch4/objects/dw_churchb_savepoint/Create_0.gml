timer = 0;
con = 0;
init = 0;
krmark = scr_marker_ext(999, 999, spr_pxwhite, 2, -2);
sumark = scr_marker_ext(999, 999, spr_pxwhite, 2, -2);
if (scr_debug())
    scr_setparty(1);
layer_set_visible("DEBUG_ASSETS", 0);
if (scr_debug())
{
    if (keyboard_check(ord("P")))
    {
    }
}
