event_inherited();
if (i_ex(body))
    inuse = body.myinteract;
if (inuse != 0)
{
    if (i_ex(obj_mainchara))
        setxy(obj_mainchara.x + (obj_mainchara.sprite_width / 2), obj_mainchara.y + (obj_mainchara.sprite_height / 2));
}
mylight.x = x;
mylight.y = y;
