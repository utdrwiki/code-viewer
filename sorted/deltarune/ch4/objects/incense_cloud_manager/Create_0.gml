cloud_surface = -1;
turn_direction = choose(-1, 1);
timer = 0;
cloud_approach = 0;
movedir_1 = choose(1, -1);
movedir_2 = choose(1, -1);
contraction = 1;
sameattack = 0;
power_val = 0;
sin_power = 1 + (sin(timer * 0.05) * power_val);
cos_power = 1 - (sin(timer * 0.05) * power_val);
depth += 1;
